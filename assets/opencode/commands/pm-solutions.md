---
description: "Generate and compare alternative responses to one product opportunity"
agent: build
---

Load the project-local `pm-solutions` skill with OpenCode's skill tool and follow it
exactly.

Opportunity slug or context: $ARGUMENTS

Do not auto-select a solution. A human-selected candidate still enters the
authoritative workflow through `/opsx-pm`.
