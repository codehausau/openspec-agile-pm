---
description: "Map a product opportunity, test assumptions, or prepare a user interview"
agent: build
---

Load the project-local `pm-discovery` skill with OpenCode's skill tool and follow it
exactly.

Discovery question or mode: $ARGUMENTS

Treat any `.nanopm/` output as local research. Do not create OpenSpec artifacts or
implementation code. Route a pursue decision through `/opsx-pm`.
